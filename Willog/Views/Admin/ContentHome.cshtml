@{
    Layout = "Admin/Layout.cshtml";
}
<div class="content" id="content">
    <div class="container">
        <ul class="shortcut-do">
            <li id="do_add_post">
                <a type="button" class="button" href="/admin/content/@Model.type/add"><span class="button-text">New Entries</span></a>
            </li>
        </ul>
        <ul class="post-list">
            @foreach (var post in @Model.postList)
            {
                <li>
                    <strong>[@post.Created.ToString("yyyy-MM-dd")]</strong><a href="/admin/@post.Type/edit/@post.Id">@post.Title</a>
                    <div class="do">
                        <a href="/admin/content/@post.Type/edit/@post.Id">编辑</a>
                        <a href="/admin/content/@post.Type/del/@post.Id">删除</a>
                    </div>
                </li>
            }
        </ul>
        <div class="navigation">
        @if (@Model.hasPrevious)
        {
            <div class="alignleft"><a href="/admin/content/@Model.type?p=@(((int)Model.currentPage) - 1)">« Previous Entries</a></div>
        }
        @if (@Model.hasNext)
        {
            <div class="alignright"><a href="/admin/content/@Model.type?p=@(((int)Model.currentPage) + 1)">Next Entries »</a></div>
        }
        </div>
    </div>   
</div>